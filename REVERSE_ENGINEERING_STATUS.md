# 逆向工程状态

## 原始证据

输入为三个完整 Flash 备份的前 `0x3000` 字节。原件位于本工程之外，分析和测试不会修改它们。

| 配置 | MCU | 原厂 BL SHA-256 |
|---|---|---|
| C23 | GD32F303，256 KiB | `edb7438733bacf3e68fc20b0821bf423ac1810e2eb1403a7b56e8a3ee15eceff` |
| C13 | GD32F303，128 KiB | `c99a5edde286811a44dc85554b0d5fccc08017b476adeb02d5db6e1308ebda9b` |
| C10 | GD32E230，64 KiB | `88bcb813f92ba1d62a22d3843cc0eb0f903e62164c862842152b143ff5546158` |

三份镜像在 Flash 偏移零和 `0x3000` 处分别有独立向量表。C23、C13、C10 的原厂复位处理函数地址依次为 `0x08000440`、`0x0800047c`、`0x0800049c`（已清除 Thumb 位），初始栈指针依次为 `0x2000c000`、`0x20008000`、`0x20002000`。原厂 16 项核心向量区在偏移 `0x40` 结束。

## 置信度分类

### 高置信度，已在源码中重现

- Flash 布局、应用跳转、命令编号、响应封包、版本组成和应用有效性检查；
- 元数据偏移、CRC 算法、各配置的数据块大小和 Flash 容量；
- 长度包最多五次尝试，每个已接收数据块最多三次写入尝试；
- 无效 `start_app` 返回 `1f`；有效时返回 `75`，延时 1000 ms 后跳转；
- 校验失败中止本轮传输，由主机从头重启；
- 最后一个短块采用“读整页—合并—擦除—重写”，保留页尾；
- F303：HXTAL/2×30=120 MHz，两个 APB 均 60 MHz；E230：HXTAL×9=72 MHz，APB 不分频；
- 115200 波特率分频值分别为 `521` 和 `625`，SysTick 为 1 ms；
- 晶振和 Flash 忙等待有限时，Flash 上限约 `0xf0000` 次循环；
- USART0 使用 PA9/PA10，USART1 使用 PA2/PA3；E230 选择复用功能 1。

### 中等置信度，已作等价实现

- 上电等待应用的 15 秒窗口；
- 命令/包接收的 500 ms 超时；
- 双 UART 扫描及发现后锁定端口；
- 写入前擦除及写入后回读验证。

### 仍需硬件验证

- 生产板实际晶振频率、实测 UART 波特率及 PCB 实际引出的 UART；
- Flash 错误清除、看门狗、复位原因及跳转前中断清理；
- 末块页尾保留和擦写期间断电后的行为；
- 畸形命令和无效应用的精确时序及响应。

当前重建镜像是研究产物，不是发布或救砖镜像。编译成功只证明源码完整且布局满足约束，不能证明已可安全上机。

重建版有一项有意加固：拒绝超过物理 Flash 容量的应用长度。原厂 BL 未发现明确软件上界检查，似乎依赖 Flash 外设报错；保留此差异是为了防止越界写入。

## BL 与主机升级工具交叉核对（2026-09-03）

V1.1.0.50 与 V1.1.0.57 的 `mcu_util` 逐字节相同，SHA-256 为 `0de7a81ecf31c76fe07d4d5be27f83345dca074806014df69bbe88e0407a23de`。其 ARM 状态机证实：

- 首次发送 `75`；控制包为 `00 ff`、`01 fe`、`02 fd`、`03 fc`；
- 应用长度以四字节小端序发送，后接校验字节；
- 固件读取受栈上 `0x4400` 缓冲区限制；
- `1f` 使文件位置回退并从头重启升级；`20` 进入 `start_app`（除非指定 `--no-startup`）；`21` 按 Flash 失败终止；
- 主机在开始升级、确认长度和确认启动三个阶段均接受 `75` 并验证反和校验。

三份 BL 命令循环独立证实相同的四命令分发表和重试行为。F303 与 E230 在发现阶段均以 115200 初始化 USART0（`0x40013800`）和 USART1（`0x40004400`）。

## 已恢复的函数及外设映射

下列为原厂镜像地址（已清除 Thumb 位），不是净室重建版地址：

| 功能 | GD32F303 原厂镜像 | GD32E230 原厂镜像 |
|---|---:|---:|
| 复位处理 | C23 `08000440`，C13 `0800047c` | C10 `0800049c` |
| 保留页尾的 Flash 写入 | `080005e0` | `08000678` |
| 应用交接/跳转 | `08000700` | `080007d0` |
| USART0 初始化 | `080007f4` | `080008f4` |
| USART1 初始化 | `0800086c` | `0800099c` |
| 系统时钟初始化 | `08000b74` 附近 | `08000d30` 附近 |

相关外设地址：RCU `40021000`、F303 PMU `40007000`、FMC `40022000`、USART0 `40013800`、USART1 `40004400`。F303 原厂实现读取 `1ffff7e0` 容量字，超过 512 KiB 时可选择第二 Flash bank。重建版现已恢复该选择逻辑：仅当容量字大于 512 KiB 且写入地址达到 `08080000` 时切换到 bank 1；三个现有目标仍自然使用 bank 0。

GPIO 参数证明 USART0 使用 PA9/PA10（掩码 `0x200/0x400`），USART1 使用 PA2/PA3（掩码 `0x4/0x8`）。这能证明固件配置，不能证明每块生产 PCB 都引出了全部四脚。

## 重建验证快照

三个配置均可用 GCC 9.2.1 编译并装入 `0x3000` 区域：

工程现已引入 Kconfig/menuconfig。`make menuconfig` 可选择 C23、C13、C10，
并配置上电握手窗口和数据包接收超时；所选板型会自动决定 Cortex-M 架构、
平台实现、Flash 参数、RAM 容量和板卡标识。`make profiles` 仍可一次构建
全部三个固定配置，用于交叉核对和自动回归。

| 配置 | 大小（字节） | 重建版 SHA-256 |
|---|---:|---|
| C23 | 12172 | `9c5f08b75d1e72347900cec18314d27ad4b9686466f81615dd429c55e019689d` |
| C13 | 12172 | `8bb3e5fb1c4379436d06f3262382df21e5e37b97449c5e7b5e28acf83e7c4827` |
| C10 | 12172 | `d17f4053ed456dad32caf42cee28e9513c299c6edeb09eadfccc12e0fc30f175` |

测试会读取未经修改的完整备份，验证 BL 哈希、初始栈、复位向量、板卡标识、应用版本、声明长度和 CRC。重建哈希仅用于标识，不表示与原厂二进制逐字节一致。

上述 BIN 文件长度包含从代码末尾到固定板卡标识偏移 `0x2f80` 之间的填充；
链接器报告的实际代码/只读数据占用分别为 C23/C13 的 1876 字节和 C10 的
1964 字节。固定该段后，重建版与原厂一样可直接在 BL 偏移 `0x2f80`
读取 12 字节标识。

C10、C13、C23 的应用长度依次为 `28616`、`30136`、`35940`，CRC 依次为 `8c8e`、`f5da`、`b78a`，均通过恢复算法复核。

## 原厂开发工具链归因

当前最合理的归因是：原厂 BL 为基于 GigaDevice GD32 标准外设库（SPL）的
独立裸机工程，很可能使用 Keil MDK 与 ARM Compiler 5（`armcc`）构建。
这是概率判断，不是已由工程文件或编译器标记直接证明的结论。

支持 Keil/ARMCC5 的证据：

- GPIO、RCU、USART、FMC 等底层函数的参数和调用结构与 GigaDevice SPL
  接口高度一致，符合 GD32 官方 Keil 示例工程的常见组织方式；
- 12 字节板卡标识被放在绝对 Flash 地址 `0x08002f80`，这种布局很像
  ARMCC 的绝对地址对象（例如 `__attribute__((at(...)))` 或生成的
  `.ARM.__at_*` 段），也可由 Keil scatter 文件实现；
- Thumb/Thumb-2 函数尾调用、寄存器保存和小型库例程的形态更接近旧版
  ARMCC5 优化输出，没有看到明显的 LLVM/armclang 运行库指纹；
- GD32F303 与 GD32E230 两版具有相同的应用层结构，但分别链接各自的 SPL
  外设实现，符合一个 Keil 多目标工程或两套近似工程。

不能据此排除 GNU Arm GCC：GCC 配合链接脚本也能生成相同的固定地址段，
而且创想公开的 Hi/K2 Klipper MCU 工程明确使用 `arm-none-eabi-gcc`。不过
这些公开仓库只包含 Klipper 应用及 GD32 支持代码，没有这份 12 KiB 私有 BL
工程，所以不能反推 BL 必然使用 GCC。扁平 BIN 已丢失 ELF 的 `.comment`、
`.ARM.attributes`、符号表和构建路径，现阶段无法进一步确定编译器版本。

armclang/LLVM 和 IAR 的可能性较低：目前没有发现其特有运行库、启动代码或
段布局指纹。也没有 RT-Thread 内核、FinSH/MSH、线程调度、IPC、对象容器、
版本/版权字符串或堆初始化痕迹；控制流是初始化后轮询双 UART 的简单裸机
循环。因此可以高置信度排除“BL 运行在 RT-Thread 上”，但仅凭二进制不能
排除开发者曾用 RT-Thread Studio 作为 IDE 并以 GCC 构建裸机工程。

综合排序：Keil MDK + ARMCC5 为高概率候选，GNU Arm Embedded GCC 为次要
候选，armclang/IAR 为低概率候选，RT-Thread 作为运行时基本排除。若后续能
取得原厂 `.elf/.axf/.map/.lst`、Keil 工程文件、构建日志或对象文件，才可能
把工具链归因提升为确定结论。

## C 状态机动态执行闭环（2026-09-04）

此前协议回归主要由 Python 金值和 C 源码结构断言组成，能够证明算法和常量，但没有
真正执行 `src/protocol.c` 的命令循环。现新增 `tests/protocol_host_harness.c` 和
`make hosttest`：在主机进程的 `0x08003000` 映射一段模拟应用 Flash，使用脚本化
UART 输入、TX 捕获、可控 Flash 写失败和应用跳转钩子，直接运行同一份 `bl_run()`、
`bl_application_valid()`、`bl_checksum()` 与 `bl_crc16_update()`。

C23、C13、C10 三种编译配置各通过 12/12 条动态路径：扇区代码；有效和 CRC 无效的
版本响应；有效应用延时后跳转和无效应用拒绝；一次写入成功；前两次 Flash 写失败、
第三次成功；连续三次失败；数据包校验错误不触碰 Flash；超过物理容量的长度被拒绝。
另两条路径确认长度包校验失败后可以在下一次恢复，以及连续五次失败后退出本轮更新。
测试同时确认锁定 UART 不漂移、500 ms 读超时参数传入平台层、成功写入字节完全一致
以及状态包反和校验。

为让同一 C 文件在 64 位主机严格 `-Werror` 下编译，仅把内部地址转换显式经过
`uintptr_t`；ARM 目标重新构建后代码/只读数据占用仍为 C23/C13 1776 字节、C10
1964 字节，三份 BIN SHA-256 与上表完全不变。因此新增测试没有改变 MCU 产物。
这一闭环提高了协议复刻置信度，但仍不能替代真实 UART 时序、Flash 擦写及断电试验。
